import VectorLayer = ol.layer.Vector;

export class OlLayers {
  airport: VectorLayer;
  runway: VectorLayer;
  direction: VectorLayer;
  threshold: VectorLayer;
  individual: VectorLayer;
  building: VectorLayer;
  wiring: VectorLayer;
  stopway:VectorLayer;
  clearway: VectorLayer;
}
